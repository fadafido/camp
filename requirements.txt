# CAMP — pip requirements (clean, fully pinned)
#
# Python : CPython 3.13.5   |   CPU-only (no CUDA/MPS)   |   seed 42
#
# Versions match environment-lock.txt (the bit-for-bit reproducibility lock).
# Two install notes:
#   * torch / torch-geometric: install the CPU build. You may need the CPU wheel
#     index, e.g.
#       pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
#       pip install torch-geometric==2.7.0
#   * xgboost on macOS arm64 needs the OpenMP runtime (libomp) on the loader path
#     BEFORE Python launches (only the gradient-boosted baseline uses it):
#       brew install libomp
#       export DYLD_LIBRARY_PATH="/opt/homebrew/opt/libomp/lib:$DYLD_LIBRARY_PATH"

# Scientific Python core
numpy==2.3.2
pandas==2.3.1
pyarrow==24.0.0
scipy==1.16.1
scikit-learn==1.6.1
matplotlib==3.10.5
networkx==3.6.1

# Scraping / catalogue ingestion
requests==2.32.4
beautifulsoup4==4.13.4
lxml==6.0.0
pdfminer.six==20260107

# RL / GNN stack (CPU)
torch==2.12.0
torch-geometric==2.7.0
stable-baselines3==2.8.0
sb3-contrib==2.8.0
gymnasium==1.2.3

# Gradient-boosted baseline (needs libomp — see note above)
xgboost==2.1.4
