pandas>=2.0,<3.0
pyarrow>=14.0  # parquet engine for the flat enrolments table (Phase A)
numpy>=1.24,<2.0
scikit-learn>=1.3,<2.0
matplotlib>=3.7,<4.0
networkx>=3.0,<4.0
pyyaml>=6.0,<7.0
pdfminer.six>=20240706
requests>=2.31,<3.0
beautifulsoup4>=4.12,<5.0
lxml>=4.9,<6.0
tqdm>=4.65,<5.0
jupyter>=1.0,<2.0
ipykernel>=6.25,<7.0
# PyTorch (CPU build) and PyTorch Geometric are installed separately — install the
# CPU PyTorch wheel for your platform, then run `pip install torch-geometric`.
# XGBoost (==2.1.4, CPU) is installed separately — it needs the OpenMP runtime
# (brew install libomp) and DYLD_LIBRARY_PATH="/opt/homebrew/opt/libomp/lib:..."
# set before launch so libomp.dylib resolves at import. Used only by the
# gradient-boosted baseline (src/models/exp_gradient_boosted_xgb.py).
