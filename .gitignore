# Python caches
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# OS / editor cruft
.DS_Store

# NOTE: data/ and pipeline outputs are intentionally NOT ignored — the dataset
# and results are to be committed once regenerated by the fresh pipeline.

# transient run logs
logs/
