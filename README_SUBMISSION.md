# CAMP / CAP-Bench — Submission provenance

AI503 Machine Learning Assignment 2 · BUiD MSc AI.

## Reported results — tag `v1.1-ai503-a2` (commit `3f124ff`)

The results reported in the paper are **frozen at tag `v1.1-ai503-a2`**
(commit `3f124ff`), which the paper cites. **Every metric in the paper traces
to the result files at that tag.**

## Final consistent repository — tag `v1.2-ai503-a2-final`

The complete, internally consistent final repository state is tagged
**`v1.2-ai503-a2-final`** (the fixes landed in commits `cc0e9e6` and `284a18a`).
Relative to `v1.1-ai503-a2` it adds **only**:

- the "Paper ↔ repository artefact mapping" section in `README.md`;
- docstring synchronisation (`make_figures.py` → F1–F11; `make_tables.py` →
  the ten numbered tables plus the unnumbered Section 8.2 comparison);
- the datasheet UNC wording ("BS Economics"); and
- a cosmetic UNC programme-label correction in the generated `T1` dataset table
  ("BS Economics").

It changes **no reported metric, result JSON, dataset, figure content,
simulator, or model/engine code.**

## Not part of the submission

The `exp/20k-students` branch is an **unrelated private experiment** (a
scale-robustness trial) and is **not part of this submission**.
