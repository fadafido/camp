# UNC raw source

No raw source artefact was carried over for UNC. The previous project held only
*extracted* outputs for UNC (`extracted_courses.json` / `extracted_program.json`),
which are deliberately excluded from this fresh rebuild. The UNC catalogue is to be
re-fetched/re-extracted from its public 2024-2025 source as part of the rebuild.
